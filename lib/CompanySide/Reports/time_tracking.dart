import 'package:flutter/material.dart';

class TimeTrackingView extends StatelessWidget {
  const TimeTrackingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Time Tracking"),
      ),
    );
  }
}
