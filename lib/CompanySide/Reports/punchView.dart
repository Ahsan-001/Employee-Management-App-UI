import 'package:flutter/material.dart';

class PunchView extends StatelessWidget {
  const PunchView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Punch View"),
      ),
    );
  }
}
